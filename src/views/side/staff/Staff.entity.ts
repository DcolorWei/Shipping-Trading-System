interface Staff {
    staffId: number | null,
    sex: 'f' | 'm' | 'o' | null,
    phone: string | null,
    email: string | null,
    fax: string | null,
    joinDate: string | null,
    birthDay: string | null,
    country: string | null,
    city: string | null,
    address: string | null
}

export {
    Staff
}